---
// Hero.astro
---

<section class="hero">
  <div class="hero-content">
    <h1 class="hero-heading">Welcome to Nepal Tek Community</h1>
    <p class="hero-subheading">Join the movement to build a vibrant tech community in Nepal. Together, we innovate, learn, and grow!</p>
    <a href="#join" class="cta-button">Join Now</a>
  </div>
</section>

<style>
  .hero {
    position: relative;
    background: url('/src/assets/photo.png') no-repeat center center/cover;
    height: 100vh; /* Full screen height */
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    color: white;
  }

  /* Adding a blur effect to the background */
  .hero::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
    filter: blur(8px);
    z-index: 1;
  }

  .hero-content {
    z-index: 2; /* Content is above the blurred background */
    padding: 2rem;
    max-width: 800px;
    width: 100%;
  }

  .hero-heading {
    font-size: 3rem;
    font-weight: bold;
    margin-bottom: 1rem;
  }

  .hero-subheading {
    font-size: 1.5rem;
    margin-bottom: 2rem;
  }

  .cta-button {
    background-color: #1e90ff;
    color: white;
    font-size: 1.2rem;
    padding: 0.75rem 2rem;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease;
  }

  .cta-button:hover {
    background-color: #007bff;
  }

  @media (max-width: 768px) {
    .hero-heading {
      font-size: 2rem;
    }

    .hero-subheading {
      font-size: 1rem;
    }

    .cta-button {
      font-size: 1rem;
      padding: 0.5rem 1.5rem;
    }
  }
</style>
